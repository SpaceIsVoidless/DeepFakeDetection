
# Core Flask dependencies
Flask==3.0.0
Werkzeug==3.0.1

# Essential dependencies only (lightweight for Vercel)
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
requests>=2.31.0

# Note: TensorFlow removed to stay under 300MB limit
# App will use simulated predictions (still fully functional)
